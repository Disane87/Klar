# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.turbo/
coverage/

# Angular
apps/web/.angular/
apps/web/dist/

# Environment
.env
.env.local
.env.*.local

# JWT Keys (generated via pnpm --filter @klar/api keys:generate)
apps/api/keys/

# IDE (keep shared settings, ignore personal workspace file)
.vscode/*.code-workspace

# OS
.DS_Store
Thumbs.db

# Prisma test DB
*.db
*.db-journal

# Seed mit realen Daten — nur lokal, nie einchecken
prisma/seed.ts

# Ruflo / swarm state
ruvector.db
agentdb.rvf
agentdb.rvf.lock
.swarm/
.claude-flow/

# Claude local config & lock files
.claude/ruflo.config.json
.claude/scheduled_tasks.lock
.playwright*/
screen-*

# Playwright
apps/web/test-results/
apps/web/e2e/.auth/

# Playwright / test screenshots (root-level, never commit)
buchungen-*.png
fixkosten-*.png
*.png

# App assets (tracked despite *.png rule above)
!apps/web/src/*.png
!apps/web/src/icons/*.png
.gstack/
.superpowers

# Build artifacts (TypeScript compiled output in scripts/)
apps/api/scripts/*.js
apps/api/scripts/*.js.map
apps/api/scripts/*.d.ts
apps/api/vitest.config.js
apps/api/vitest.config.js.map
apps/api/vitest.config.d.ts
