# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.turbo/
coverage/

# Angular
apps/web/.angular/
apps/web/dist/

# Environment
.env
.env.local
.env.*.local

# JWT Keys (generated via pnpm --filter @klar/api keys:generate)
apps/api/keys/

# IDE (keep shared settings, ignore personal workspace file)
.vscode/*.code-workspace

# OS
.DS_Store
Thumbs.db

# Prisma test DB
*.db
*.db-journal

# Seed mit realen Daten — nur lokal, nie einchecken
prisma/seed.ts

# Ruflo / swarm state
ruvector.db
agentdb.rvf
agentdb.rvf.lock
.swarm/
.claude-flow/
.playwright*/
screen-*

# Playwright
apps/web/test-results/
apps/web/e2e/.auth/

# Playwright / test screenshots (root-level, never commit)
buchungen-*.png
fixkosten-*.png
*.png
.gstack/
