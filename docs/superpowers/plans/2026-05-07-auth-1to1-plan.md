# Auth Pages 1:1 Implementation Plan

> Subagent-driven. Continuous execution.

**Goal:** Polish login / register / verify-email / oauth-consent / onboarding / join pages to bundle PageAuth — two-pane brand+form layout, Fraunces hero, status strip with security chips, mobile-collapsed brand pane.

---

## Tasks

### Task 1: Brand-pane shared component

**File:** `apps/web/src/app/shared/ui/klar-auth-brand-pane.component.ts` (new)

Standalone component reusable across all auth pages. Props: none (the brand-pane is identical bundle content). Renders:

```html
<aside class="hidden lg:flex flex-col justify-between bg-(--bg-1) border-r border-(--line) px-(--s-12) py-(--s-12) min-h-dvh">
  <klar-wordmark [size]="28" />
  <div class="flex flex-col gap-(--s-4)">
    <span class="eyebrow">Ein Haushalt — keine Wolke</span>
    <h1 class="m-0 leading-[1.05] tracking-[-0.025em]"
        style="font-family: var(--font-display); font-size: 48px; font-weight: 500;">
      Privatfinanzen,<br/>
      <span class="italic font-normal text-(--accent)">klar genug</span> für<br/>zwei Erwachsene.
    </h1>
    <p class="text-[14px] text-(--fg-2) max-w-[360px] leading-[1.55]">
      Selbstgehostet auf deinem NAS oder Mini-PC. Keine Telemetrie, keine Bank-API,
      kein Algorithmus, der für dich denkt.
    </p>
  </div>
  <div class="flex flex-wrap gap-(--s-5) text-[10px] uppercase tracking-[0.14em] text-(--fg-2)">
    <span class="inline-flex items-center gap-1.5"><klar-icon name="shield" [size]="12" /> ARGON2ID</span>
    <span class="inline-flex items-center gap-1.5"><klar-icon name="lock" [size]="12" /> 100% LOKAL</span>
    <span class="inline-flex items-center gap-1.5"><klar-icon name="key" [size]="12" /> RS256 JWT</span>
  </div>
</aside>
```

**Commit:** `feat(auth): shared klar-auth-brand-pane component`

---

### Task 2: Apply brand-pane to all auth pages

**Files:** all under `apps/web/src/app/pages/{login,register,verify-email,onboarding,oauth-consent,join,auth-callback}/`. For each:

1. Replace existing inline brand pane with `<klar-auth-brand-pane />`.
2. Wrap form in `<main class="flex-1 flex items-center justify-center p-(--s-6) lg:p-(--s-12)">`.
3. Outer container: `<div class="grid grid-cols-1 lg:grid-cols-[440px_1fr] min-h-dvh bg-(--bg)">`.

Form pane chrome stays — only the surrounding layout changes.

**Commit:** `feat(auth): wire brand-pane into all 7 auth pages`

---

### Task 3: README + verification

```
| **🔐 Authentication UX** | Two-pane bundle layout: brand pane left (Fraunces 'klar genug' hero + ARGON2ID/100% LOKAL/RS256 chips) hidden on mobile, form pane right; applied to login / register / verify-email / oauth-consent / onboarding / join / auth-callback |
```

Triple build green. Commit: `docs(readme): document auth two-pane layout`
